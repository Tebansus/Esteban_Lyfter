#!/usr/bin/env python
# coding: utf-8

# In[1]:


# Define the lists
first_list = ['Hay', 'en', 'que', 'iteracion', 'indices', 'muy']
second_list = ['casos', 'los', 'la', 'por', 'es', 'util']

# Print the lists interchangeably 
for i in range(len(first_list)):
    print(first_list[i], second_list[i])

